name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        pg_version: ['18', '17', '16', '15', '14']

    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Start PostgreSQL ${{ matrix.pg_version }}
        run: |
          docker run -d --name pg_current-test \
            -e POSTGRES_PASSWORD=pg_current_test \
            -e POSTGRES_DB=pg_current_test \
            -p 5432:5432 \
            postgres:${{ matrix.pg_version }}

          # Wait for PG to be ready
          for i in $(seq 1 30); do
            docker exec pg_current-test pg_isready -U postgres && break
            sleep 1
          done || { echo "PG not ready after 30 seconds"; exit 1; }

      - name: Build pg_current
        run: bash build/transform.sh

      - name: Install pg_current
        run: |
          PGPASSWORD=pg_current_test psql -h localhost -U postgres -d pg_current_test \
            -v ON_ERROR_STOP=1 -f sql/pg_current.sql

      - name: Run regression tests
        run: |
          PGPASSWORD=pg_current_test psql -h localhost -U postgres -d pg_current_test \
            -v ON_ERROR_STOP=1 -f tests/run_all.sql

      - name: Run acceptance tests
        run: |
          PGPASSWORD=pg_current_test psql -h localhost -U postgres -d pg_current_test \
            -v ON_ERROR_STOP=1 -f tests/acceptance/run_acceptance.sql

      - name: Test install idempotency
        run: |
          # Re-run install -- should produce no errors
          PGPASSWORD=pg_current_test psql -h localhost -U postgres -d pg_current_test \
            -v ON_ERROR_STOP=1 -f sql/pg_current.sql
          # Verify everything still works
          PGPASSWORD=pg_current_test psql -h localhost -U postgres -d pg_current_test \
            -v ON_ERROR_STOP=1 -f tests/test_install_idempotency.sql

      - name: Test uninstall
        run: |
          PGPASSWORD=pg_current_test psql -h localhost -U postgres -d pg_current_test \
            -v ON_ERROR_STOP=1 -f sql/pg_current_uninstall.sql
          # Verify schema is gone
          result=$(PGPASSWORD=pg_current_test psql -h localhost -U postgres -d pg_current_test \
            -v ON_ERROR_STOP=1 -tAc "select count(*) from pg_namespace where nspname = 'pg_current'")
          if [ "$result" != "0" ]; then
            echo "FAIL: pg_current schema still exists after uninstall"
            exit 1
          fi
          echo "PASS: pg_current schema removed after uninstall"

      - name: Cleanup
        if: always()
        run: docker rm -f pg_current-test

  client-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Start PostgreSQL 18
        run: |
          docker run -d --name pg_current-client-test \
            -e POSTGRES_PASSWORD=pg_current_test \
            -e POSTGRES_DB=pg_current_test \
            -p 5432:5432 \
            postgres:18

          for i in $(seq 1 30); do
            docker exec pg_current-client-test pg_isready -U postgres && break
            sleep 1
          done || { echo "PG not ready after 30 seconds"; exit 1; }

      - name: Build pg_current
        run: bash build/transform.sh

      - name: Install pg_current
        run: |
          PGPASSWORD=pg_current_test psql -h localhost -U postgres -d pg_current_test \
            -v ON_ERROR_STOP=1 -f sql/pg_current.sql
          PGPASSWORD=pg_current_test psql -h localhost -U postgres -d pg_current_test \
            -v ON_ERROR_STOP=1 -c "select pg_current.create_queue('smoke_py'); select pg_current.create_queue('smoke_go'); select pg_current.create_queue('smoke_ts');"
          PGPASSWORD=pg_current_test psql -h localhost -U postgres -d pg_current_test \
            -v ON_ERROR_STOP=1 -c "select pg_current.force_tick('smoke_py'); select pg_current.force_tick('smoke_go'); select pg_current.force_tick('smoke_ts'); select pg_current.ticker();"

      - name: Python smoke
        run: |
          python3 -m pip install -U pip
          python3 -m pip install -e clients/python[dev]
          pytest -q clients/python/tests/test_smoke.py

      - name: Go smoke
        run: |
          cd clients/go
          go test -run TestSmoke ./...

      - name: TypeScript smoke
        run: |
          cd clients/typescript
          npm install
          npm run check
          npx tsx src/smoke.ts

      - name: Cleanup client smoke DB
        if: always()
        run: docker rm -f pg_current-client-test

  verify:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Build
        run: bash build/transform.sh

      - name: Verify SECURITY DEFINER search_path
        run: |
          # Check that no SECURITY DEFINER function lacks SET search_path
          if grep -rn -i 'security definer' build/output/ \
             | grep -iv 'set search_path'; then
            echo "FAIL: SECURITY DEFINER functions missing SET search_path"
            exit 1
          fi
          echo "PASS: All SECURITY DEFINER functions have SET search_path"

      - name: Verify no remaining pgq references
        run: |
          if grep -rn 'pgq\.[a-zA-Z_]' build/output/ \
             | grep -v '^\s*--' \
             | grep -v '^[^:]*:[0-9]*:\s*--'; then
            echo "FAIL: Remaining pgq. references in output"
            exit 1
          fi
          echo "PASS: No remaining pgq. references"

      - name: Verify no remaining txid references
        run: |
          if grep -rn -E 'txid_(current|snapshot|visible)' build/output/; then
            echo "FAIL: Remaining txid_ references"
            exit 1
          fi
          echo "PASS: No remaining txid_ references"
